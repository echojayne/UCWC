# Data

`radio_maps/` stores small checked-in fixtures or prepared RadioMapSeer subsets.
Large archives such as `RadioMapSeer.zip` stay local and are ignored by git.

`semantic_catalogs/` is reserved for generated or measured semantic
configuration tables, such as RIDAS-style experience catalogs.
